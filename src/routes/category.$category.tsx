import { createFileRoute } from "@tanstack/react-router"; import { ListingPage } from "../components/pages";
export const Route = createFileRoute("/category/$category")({ head:({params})=>({meta:[{title:`${params.category} — JustBay`},{name:"description",content:`Shop ${params.category} from verified local sellers on JustBay.`},{property:"og:title",content:`${params.category} — JustBay`},{property:"og:description",content:"Local products, clear prices and reliable delivery."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}), component:Page }); function Page(){return <ListingPage category={Route.useParams().category}/>}